---
name: build
description: Run project build. Report terse list of errors and warnings only — no fixes, no prose.
---

Detect build command (npm/cargo/go/make/etc), run, output `errors:` then `warnings:` as terse `file:line — message` list. No fixes. No prose.
